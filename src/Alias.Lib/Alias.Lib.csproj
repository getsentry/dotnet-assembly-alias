<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <PackageId>Sentry.AssemblyAlias.Lib</PackageId>
    <TargetFrameworks>net10.0;net48;netstandard2.0</TargetFrameworks>
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
    <GeneratePackageOnBuild Condition="$(Configuration) == 'Release'">true</GeneratePackageOnBuild>
    <DebugType>embedded</DebugType>
    <DebugSymbols>true</DebugSymbols>
    <SignAssembly>True</SignAssembly>
    <AssemblyOriginatorKeyFile>$(SolutionDir)key.snk</AssemblyOriginatorKeyFile>
    <!-- Polyfill nags to add intermediate net47x targets for full polyfill coverage; not worth it here. -->
    <NoWarn>$(NoWarn);PolyfillTargetsForNuget</NoWarn>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Polyfill" Version="10.10.0" PrivateAssets="all" />
    <None Include="$(SolutionDir)icon.png" Pack="true" PackagePath="\" />
    <Reference Include="Mono.Cecil" HintPath="..\Lib\Mono.Cecil.dll" />
    <Reference Include="Mono.Cecil.Pdb" HintPath="..\Lib\Mono.Cecil.Pdb.dll" />
    <Reference Include="Mono.Cecil.Rocks" HintPath="..\Lib\Mono.Cecil.Rocks.dll" />
    <PackageReference Include="Microsoft.SourceLink.GitHub" Version="10.0.300" PrivateAssets="All" Condition="$(Configuration) == 'Release'" />
    <Using Remove="System.Net.Http" />
    <EmbeddedResource Include="netstandard.dll">
      <CopyToOutputDirectory>Never</CopyToOutputDirectory>
    </EmbeddedResource>
  </ItemGroup>

</Project>
